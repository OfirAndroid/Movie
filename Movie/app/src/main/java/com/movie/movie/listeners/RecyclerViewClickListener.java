package com.movie.movie.listeners;

import android.view.View;

/*
 * The RecyclerViewClickListener is used for passing onClick event happened in the RecyclerView
 * */
public interface RecyclerViewClickListener {

    void onClick(View view); // this method is called when a click event accord
}